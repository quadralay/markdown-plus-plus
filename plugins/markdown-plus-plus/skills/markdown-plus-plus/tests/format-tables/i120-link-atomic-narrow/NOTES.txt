#120 fixture: link atomicity + soft width rule.

args.txt sets --max-cell-width 12 so the link (21 chars) is wider than the requested cap,
forcing the wrap/widen decision.

"[the guide](guide.md)" is matched by the inline-link token class and is atomic -- never
split. Per the #120 soft width rule, the Link column floors at its widest atomic token (the
21-char link), so the whole column widens to 21 and every row in the column aligns at that
width. Prose ("See", "here.") wraps around the intact link on its own continuation row.

The continuation shape follows the tool's own greedy wrap algorithm
(format-tables.wrap_tokens), extended with the link-atomic token class.
