#121 fixture: over-width no-space directive (case 2 in #121).

args.txt sets --max-cell-width 20. The directive
'<!--markers:{"Keywords":"api,rest,http"}-->' has no interior whitespace and is 43 chars,
well over the cap.

The directive is matched by the HTML-comment token class as a single atomic token, so it is
never hard-broken. The Definition column floors at the 43-char directive under the soft width
rule and the whole column widens to 43; every row aligns at that width. The following prose
("Application interface.") stays on its own continuation row.
