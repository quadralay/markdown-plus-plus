#120 fixture: list-marker continuation indentation.

args.txt sets --max-cell-width 22 so the list item wraps.

The cell begins with a "- " list marker, detected by wrap_cell / detect_content_prefix. The
item body is wrapped to (col_width - len("- ")); the first line keeps the marker and every
continuation line is indented 2 spaces (the marker width) so the wrapped remainder aligns
under the item body and still reads as part of the list item:

    - Enable the optional
      analytics feature in
      settings

This follows the prior-art detect_content_prefix() behavior offered in #120.
