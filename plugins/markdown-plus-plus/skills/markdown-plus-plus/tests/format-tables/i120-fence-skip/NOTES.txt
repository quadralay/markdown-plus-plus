#120 fixture: in-cell code-fence skip.

args.txt sets --max-cell-width 20 so the 22-char code line exceeds the cap and would be
wrapped absent fence awareness.

The Example column contains a fenced code block spanning continuation rows: "```python",
"print(\"hello, world!\")", "```". The formatter tracks fence state independently down each
column across the table's data rows (mirroring the document-level fence scanner but scoped
to a single column), so the fence delimiters and the content between them are emitted
verbatim -- never re-wrapped.

Under the soft width rule the Example column floors at the longest protected line (the
22-char code line) and the whole column widens to fit; every row aligns at width 22.
"Greeting:" (prose above the fence) is unaffected.
