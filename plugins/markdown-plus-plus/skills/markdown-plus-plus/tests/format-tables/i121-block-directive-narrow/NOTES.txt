#121 fixture: block directive comment atomicity (case 1 in #121).

args.txt sets --max-cell-width 10 so the 22-char directive is far wider than the requested
cap.

The cell line "<!-- style:Hobbies -->" is a full-line block style directive (following the
spec's Block Styles-in-cells example). It attaches to the "- Enjoys cycling" list on the
next continuation row.

The directive is matched by the HTML-comment token class and is atomic -- never split -- so
it is emitted intact on its own continuation row. Per the soft width rule the Details column
floors at the directive length (22) and the whole column widens to 22; every row aligns at
that width. The directive's position (directly above its list target) is preserved.
