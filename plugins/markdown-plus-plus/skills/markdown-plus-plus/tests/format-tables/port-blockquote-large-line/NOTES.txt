Ported verbatim (raw bytes, CRLF preserved) from
C:\Projects\epublisher-docs\test\table-with-blockquote-large-line.md.

Ported ahead of the epublisher-docs cleanup so the specimen lives in this suite before it
is archived. No frontmatter was added - it is a messy real-world specimen used as test data.
Golden = whatever format-tables.py outputs TODAY (single pass, default args), generated by
running the real formatter. It documents current behavior on deep blockquote nesting
(>, >>, >>>), '> -' combos, and links/bracket-code compounds in cells so that #120/#121
changes surface as reviewable golden diffs. NOTE: today's single-pass output is not
idempotent for this specimen; that is expected pre-#120 behavior and is not asserted here.
