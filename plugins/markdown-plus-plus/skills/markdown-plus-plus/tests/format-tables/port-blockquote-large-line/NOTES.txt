Ported verbatim (raw bytes, CRLF preserved) from
C:\Projects\epublisher-docs\test\table-with-blockquote-large-line.md.

Ported ahead of the epublisher-docs cleanup so the specimen lives in this suite before it
is archived. No frontmatter was added - it is a messy real-world specimen used as test data.
Golden = whatever format-tables.py outputs (single pass, default args), generated by running
the real formatter. It documents current behavior on deep blockquote nesting (>, >>, >>>),
'> -' combos, and links/bracket-code compounds in cells so that #120/#121 changes surface as
reviewable golden diffs.

Regenerated for the #120/#121 stage-1 change (tokenizer + structure-preserving wrap + soft
width rule). Reviewable diffs vs the pre-change golden:
- The "Use" column now re-flows under the soft width rule: it shrinks toward its floor so the
  total line width fits the default budget (max-line-width 110), given the Convention column
  is pinned wide by the 64-char blockquote-list line and the atomic
  "[Link Text](https://www.webworks.com)" floor. Previously it sat at the widest-content cap.
- Blockquote and list cells in "Use" now wrap with structure preservation: "> - ..." items
  hang under a ">   " continuation, "- ..." items hang under a two-space continuation, so the
  wrapped remainder still reads as part of the blockquote/list.
- "[Link Text](https://www.webworks.com)" is now an atomic link token (never split); it
  already fit on one line here, so its rendered position is unchanged but it is now protected.

NOTE: single-pass output remains non-idempotent for this specimen (the documented
auto-strategy limitation); the case is allowlisted in the idempotency sweep. Stage 2 (the
link-reference rewrite) will regenerate this golden again.
