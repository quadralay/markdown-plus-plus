Ported verbatim (raw bytes, CRLF preserved, no trailing newline - matching the source) from
C:\Projects\epublisher-docs\test\table-with-url.md.

Ported ahead of the epublisher-docs cleanup so the specimen lives in this suite before it
is archived. No frontmatter was added - it is a messy real-world specimen used as test data.
Golden = whatever format-tables.py outputs TODAY (single pass, default args). The specimen
contains a link already split across continuation rows by a past wrap (the exact corruption
#120 describes), ragged ~200-char columns, and $var; tokens in cells; the golden documents
current behavior so #120/#121 changes surface as reviewable golden diffs.
