Byte-sensitive case. input.md AND expected.md are written with CRLF line endings
throughout (frontmatter, prose, and every table/continuation row) and compared
byte-for-byte. Verifies the formatter preserves CRLF end-to-end - including on wrapped
continuation rows generated by --max-cell-width 20 - rather than normalizing to LF.
Both files were written as raw bytes (not via the Write tool or shell redirection) to
guarantee no line-ending re-encoding. Frontmatter is retained (also CRLF) because it
exercises CRLF preservation on passthrough content and does not interfere with the case.
