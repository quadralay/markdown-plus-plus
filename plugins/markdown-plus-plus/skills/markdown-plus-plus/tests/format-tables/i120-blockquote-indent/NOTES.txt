#120 fixture: blockquote continuation prefix (including one nested level).

args.txt sets --max-cell-width 20 so both blockquote lines wrap.

The cell is authored across two source continuation rows: logical row "Quote" whose Detail
is "> This is a fairly long quoted line", and its continuation row (blank Note cell) whose
Detail is ">> A nested inner quote line also long" (nested level).

Every continuation line carries the blockquote marker of its source line -- "> " for the
first line, ">> " for the nested line -- so the wrapped text stays inside the blockquote at
the correct nesting depth. detect_content_prefix() reports the blockquote marker (with its
single trailing space) as both the first-line and continuation prefix; wrap_cell wraps each
blockquote body to (col_width - len(marker)) and re-prefixes every line with the marker, so
no rendered line exceeds the planned column width. The literal marker is preserved as
authored (">> " not "> > ").
