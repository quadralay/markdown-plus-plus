#120 fixture: bare-URL atomicity + soft width rule.

args.txt sets --max-cell-width 15 so the 34-char URL exceeds the cap.

"https://example.com/very/long/path" is matched by the bare-URL token class and is atomic --
never hard-broken. The Link column floors at the 34-char URL per the soft width rule and the
whole column widens to fit; every row aligns at that width. "Visit" and "now." wrap around
the intact URL.

The continuation shape follows the tool's own greedy wrap algorithm, extended with the
url-atomic token class.
