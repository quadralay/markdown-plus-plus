#120 red fixture: bare-URL atomicity + soft width rule.

args.txt sets --max-cell-width 15 so the 34-char URL exceeds the cap.

Today: the URL is a plain (non-atomic) token longer than the column width, so wrap_tokens()
takes the hard-break path and chops it at width boundaries mid-character (observed:
"https://example" / ".com/very/long/" / "path now."), breaking the link.

Desired (encoded in expected.md): the URL is atomic and never hard-broken. The Link column
floors at the 34-char URL per the soft width rule and widens to fit; every row aligns at
that width. "Visit" and "now." wrap around the intact URL.

Continuation shape derived with the tool's own greedy wrap algorithm extended with a
url-atomic token class.
